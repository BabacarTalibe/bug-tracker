pipeline {
    agent any
      tools {
        go 'Go-1.25.5'
      }
    stages {
        stage('Unit Tests  -  Backend') {
            steps {
                dir('bugtracker-backend') {
                    sh 'go version'
                    sh 'go test -v ./...'
                }
            }
        }
    }
}
